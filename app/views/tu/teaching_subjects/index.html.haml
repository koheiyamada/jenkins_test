=content_for :tabmenu do
  =render 'tu/profiles/nav_tab'

%h1=title t("titles.teaching_subjects")

/=render "menu"

-if @subject_levels.empty?
  =t('common.not_found')
-else
  =render 'teaching_subjects/subject_levels'
.buttons
  =img_link_to 'common.edit', edit_tu_teaching_subjects_path