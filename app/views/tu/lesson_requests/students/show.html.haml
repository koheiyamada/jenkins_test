=content_for :subheader do
  =render 'breadcrumbs'

%h1=title t("titles.student")

=render 'menu'

=render 'student', student:@student

.buttons
  =link_button_to 'file', t('titles.lesson_reports'), tu_lesson_student_lesson_reports_path(@lesson, @student)
