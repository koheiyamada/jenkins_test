%h1=title t('titles.coaches')

-if @bss.empty?
  =t('common.not_found')
-else
  =render layout:'bss/bss_censored', locals:{bss:@bss} do |bs|
    =img_link_to 'common.detail', pa_bs_path(bs), class:'small'
