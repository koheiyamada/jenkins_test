%h1=title t('titles.students')

=render 'menu'

-if @students.blank?
  -#=t('common.not_found')
-else
  =render layout:'students/students', locals:{students:@students} do |student|
    =link_to t('common.detail'), home_pa_student_path(student), class:'btn btn-small'
