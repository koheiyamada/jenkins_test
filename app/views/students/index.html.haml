%h1=title t("titles.students")

=render "menu"

=render "shared/centered_search_box"

-if @students.blank?
  =t('common.not_found')
-else
  =render layout:"students/students", locals:{students:@students} do |student|
    =img_link_to "common.detail", {action:"show", id:student}, class:"small"
  =paginate(@students)
