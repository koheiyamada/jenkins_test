- @from_hq_bss ||= false
-if @from_hq_bss == true
  %h1=title t('titles.bank_account')
-else
  %h1=title t('titles.yucho_account_confirmation')

=render @bank_account.class.name.underscore

-if @from_hq_bss == true
  =render 'show_actions'
-else
  .buttons
    =link_to t('common.registration'), {action: 'complete'}, class: 'btn btn-primary'
