=content_for :tabmenu do
  =render "nav_tab"

%h1=title t("titles.tutors")

=render "menu"

=render "shared/search_box"

-if @tutors.blank?
  =t('common.not_found')
-else
  =render layout:"tutors/tutors", locals:{tutors:@tutors} do |tutor|
    =img_link_to "common.detail", {action:"show", id:tutor}, class:"small"
  =paginate @tutors
